# GCD of two integers

int _main(){
    int n;
    scan(n);

    int m;
    scan(m);

    while(m!=n){
        if(m>n){
            m=m-n;
        }
        else{
            n=n-m;
        }
    }
    print(m);
}
